const CDRatesMobileStyles = theme => ({
  container: {
    flex: 1,
    backgroundColor: theme.whiteBG,
  },
  mainContainer: {
    backgroundColor: theme.background,
    flex: 1,
  },
});

export default CDRatesMobileStyles;
